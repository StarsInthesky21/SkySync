node_modules
.expo
.npm-cache
dist
build
*.log
tmp/

# Secrets - never commit real credentials
.env
.env.local
.env.production
google-services.json
GoogleService-Info.plist

# Release signing keystores and private keys
credentials/*.jks
credentials/*.keystore
credentials/*.p8
credentials/*.p12
credentials/*.pem
credentials/credentials.json
credentials/google-services.json
credentials/GoogleService-Info.plist

# Expo / EAS local secrets
.easignore.local
secrets.json
serviceAccountKey.json

# Build artefacts we don't want tracked
android/
ios/
*.apk
*.aab
*.ipa
