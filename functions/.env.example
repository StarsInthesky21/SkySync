# Cloud Functions environment — set in Firebase console or via CLI.
# Secrets (API key, API secret) are handled via `firebase functions:secrets:set`.

# Public: the wss:// URL of your LiveKit SFU (from server/docker-compose.yml).
LIVEKIT_URL=wss://livekit.skysync.app
